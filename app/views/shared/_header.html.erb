<header class='top-page-header'>
    <div class='nav'>
      <%= link_to 'tabizuki',posts_path,class:'title' %> 
    </div>
    <% if user_signed_in? %>
    <ul class='second-button'>
      <li><%= link_to current_user.nickname, user_path(current_user.id), class: 'my-name'%></li>
      <li><%= link_to 'ログアウト', destroy_user_session_path, method: :delete, class: 'sign-out'%></li>
    </ul>
    <% end %>
</header> 